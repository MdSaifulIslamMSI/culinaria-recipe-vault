{
  "name": "culinaria-recipe-finder",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/mealDbApi.test.js",
    "doctor": "npm run test && npm run build"
  },
  "devDependencies": {
    "puppeteer-core": "^25.7.0",
    "vite": "^8.2.1"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.3"
  }
}
